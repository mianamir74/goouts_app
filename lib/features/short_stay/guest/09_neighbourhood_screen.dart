// Generated by Google Stitch, 3 August 2026. Source folder: 9-neighbourhood
// Cleaned and consolidated by the pass described in
// goouts/design/SHORT_STAY_WIRING_PLAN.md part 1.
// NOT WIRED. Every handler in this file is still empty. See part 2.
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';
import '../theme/stay_colors.dart';

class NeighbourhoodScreen extends StatelessWidget {
  const NeighbourhoodScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: GoOutsColors.background,
      appBar: AppBar(
        backgroundColor: Colors.white,
        elevation: 0.5,
        leading: IconButton(
          icon: const Icon(Icons.arrow_back, color: GoOutsColors.primaryBlue),
          onPressed: () => Navigator.of(context).pop(),
        ),
        title: Text(
          'Local Rewards',
          style: GoogleFonts.inter(
            fontSize: 20,
            fontWeight: FontWeight.bold,
            color: GoOutsColors.deepNavy,
          ),
        ),
        actions: [
          IconButton(
            icon: const Icon(Icons.info_outline, color: GoOutsColors.deepNavy),
            onPressed: () {},
          ),
        ],
      ),
      body: Column(
        children: [
          // Map Placeholder
          Expanded(
            flex: 4,
            child: Container(
              width: double.infinity,
              decoration: const BoxDecoration(
                image: DecorationImage(
                  image: NetworkImage('https://images.unsplash.com/photo-1526778548025-fa2f459cd5ce?auto=format&fit=crop&q=80&w=800'),
                  fit: BoxFit.cover,
                ),
              ),
              child: Stack(
                children: [
                  // Map Pins
                  Positioned(
                    top: 100,
                    left: 150,
                    child: _buildMapPin(Icons.restaurant, GoOutsColors.primaryBlue),
                  ),
                  Positioned(
                    top: 200,
                    left: 250,
                    child: _buildMapPin(Icons.local_bar, Colors.orange),
                  ),
                  Positioned(
                    bottom: 100,
                    right: 80,
                    child: _buildMapPin(Icons.coffee, Colors.brown),
                  ),
                  // Location Indicator
                  const Center(
                    child: Icon(Icons.my_location, color: Colors.blue, size: 30),
                  ),
                ],
              ),
            ),
          ),
          // Nearby Partners Section
          Expanded(
            flex: 6,
            child: Container(
              decoration: const BoxDecoration(
                color: Colors.white,
                borderRadius: BorderRadius.vertical(top: Radius.circular(24)),
              ),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  _buildFilterBar(),
                  Padding(
                    padding: const EdgeInsets.fromLTRB(16, 24, 16, 16),
                    child: Text(
                      'Nearby Partners',
                      style: GoogleFonts.inter(
                        fontSize: 18,
                        fontWeight: FontWeight.bold,
                        color: GoOutsColors.deepNavy,
                      ),
                    ),
                  ),
                  Expanded(
                    child: ListView(
                      padding: const EdgeInsets.symmetric(horizontal: 16),
                      children: [
                        _buildPartnerItem(
                          icon: Icons.restaurant,
                          title: 'The Blue Boar Grill',
                          distance: '5 mins walk',
                          type: 'Modern British',
                          cashback: '15% CASHBACK',
                        ),
                        _buildPartnerItem(
                          icon: Icons.local_bar,
                          title: 'The Anchor Tavern',
                          distance: '8 mins walk',
                          type: 'Pub & Drinks',
                          cashback: '10% CASHBACK',
                        ),
                        _buildPartnerItem(
                          icon: Icons.coffee,
                          title: 'Urban Roast',
                          distance: '3 mins walk',
                          type: 'Artisan Coffee',
                          cashback: '20% CASHBACK',
                        ),
                        _buildPartnerItem(
                          icon: Icons.shopping_bag,
                          title: 'Green Grocer Market',
                          distance: '12 mins walk',
                          type: 'Organic Store',
                          cashback: '5% CASHBACK',
                        ),
                      ],
                    ),
                  ),
                ],
              ),
            ),
          ),
        ],
      ),
      bottomNavigationBar: _buildBottomNavBar(),
    );
  }

  Widget _buildMapPin(IconData icon, Color color) {
    return Container(
      padding: const EdgeInsets.all(8),
      decoration: BoxDecoration(
        color: Colors.white,
        shape: BoxShape.circle,
        boxShadow: [
          BoxShadow(
            color: Colors.black.withValues(alpha: 0.1),
            blurRadius: 8,
            offset: const Offset(0, 2),
          ),
        ],
      ),
      child: Icon(icon, color: color, size: 20),
    );
  }

  Widget _buildFilterBar() {
    return SingleChildScrollView(
      scrollDirection: Axis.horizontal,
      padding: const EdgeInsets.all(16),
      child: Row(
        children: [
          _buildFilterChip('All', Icons.grid_view, true),
          const SizedBox(width: 8),
          _buildFilterChip('Restaurants', Icons.restaurant, false),
          const SizedBox(width: 8),
          _buildFilterChip('Pubs', Icons.local_bar, false),
          const SizedBox(width: 8),
          _buildFilterChip('Cafes', Icons.coffee, false),
        ],
      ),
    );
  }

  Widget _buildFilterChip(String label, IconData icon, bool isActive) {
    return Container(
      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 10),
      decoration: BoxDecoration(
        color: isActive ? GoOutsColors.primaryBlue : GoOutsColors.background,
        borderRadius: BorderRadius.circular(12),
        border: Border.all(
          color: isActive ? GoOutsColors.primaryBlue : Colors.grey.shade300,
        ),
      ),
      child: Row(
        children: [
          Icon(
            icon,
            size: 18,
            color: isActive ? Colors.white : GoOutsColors.deepNavy,
          ),
          const SizedBox(width: 8),
          Text(
            label,
            style: GoogleFonts.inter(
              fontSize: 14,
              fontWeight: FontWeight.w600,
              color: isActive ? Colors.white : GoOutsColors.deepNavy,
            ),
          ),
        ],
      ),
    );
  }

  Widget _buildPartnerItem({
    required IconData icon,
    required String title,
    required String distance,
    required String type,
    required String cashback,
  }) {
    return Container(
      margin: const EdgeInsets.only(bottom: 12),
      padding: const EdgeInsets.all(12),
      decoration: BoxDecoration(
        color: GoOutsColors.background,
        borderRadius: BorderRadius.circular(12),
      ),
      child: Row(
        children: [
          Container(
            padding: const EdgeInsets.all(12),
            decoration: BoxDecoration(
              color: GoOutsColors.surfaceBlue,
              borderRadius: BorderRadius.circular(8),
            ),
            child: Icon(icon, color: GoOutsColors.primaryBlue),
          ),
          const SizedBox(width: 16),
          Expanded(
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Row(
                  mainAxisAlignment: MainAxisAlignment.spaceBetween,
                  children: [
                    Text(
                      title,
                      style: GoogleFonts.inter(
                        fontSize: 15,
                        fontWeight: FontWeight.bold,
                        color: GoOutsColors.deepNavy,
                      ),
                    ),
                    Container(
                      padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
                      decoration: BoxDecoration(
                        color: GoOutsColors.cashbackBlue,
                        borderRadius: BorderRadius.circular(4),
                      ),
                      child: Text(
                        cashback,
                        style: GoogleFonts.inter(
                          fontSize: 11,
                          fontWeight: FontWeight.w800,
                          color: GoOutsColors.tickTeal,
                        ),
                      ),
                    ),
                  ],
                ),
                const SizedBox(height: 4),
                Text(
                  '$distance  •  $type',
                  style: GoogleFonts.inter(
                    fontSize: 13,
                    color: GoOutsColors.textVariant,
                  ),
                ),
              ],
            ),
          ),
        ],
      ),
    );
  }

  Widget _buildBottomNavBar() {
    return Container(
      height: 80,
      decoration: BoxDecoration(
        color: Colors.white,
        border: Border(top: BorderSide(color: Colors.grey.shade200)),
      ),
      child: Row(
        mainAxisAlignment: MainAxisAlignment.spaceAround,
        children: [
          _buildNavItem(Icons.search, 'Search', false),
          _buildNavItem(Icons.explore, 'Trips', false),
          _buildNavItem(Icons.stars, 'Rewards', true),
          _buildNavItem(Icons.person, 'Profile', false),
        ],
      ),
    );
  }

  Widget _buildNavItem(IconData icon, String label, bool isActive) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Icon(
          icon,
          color: isActive ? GoOutsColors.primaryBlue : GoOutsColors.textVariant,
        ),
        const SizedBox(height: 4),
        Text(
          label,
          style: GoogleFonts.inter(
            fontSize: 12,
            fontWeight: isActive ? FontWeight.bold : FontWeight.w500,
            color: isActive ? GoOutsColors.primaryBlue : GoOutsColors.textVariant,
          ),
        ),
      ],
    );
  }
}
