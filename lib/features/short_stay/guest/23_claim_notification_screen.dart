// Generated by Google Stitch, 3 August 2026. Source folder: 23-claim_notification
// Cleaned and consolidated by the pass described in
// goouts/design/SHORT_STAY_WIRING_PLAN.md part 1.
// NOT WIRED. Every handler in this file is still empty. See part 2.
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';
import '../theme/stay_colors.dart';

class ClaimNotificationScreen extends StatelessWidget {
  const ClaimNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: GoOutsColors.background,
      appBar: AppBar(
        backgroundColor: Colors.white,
        elevation: 0.5,
        leading: IconButton(
          icon: const Icon(Icons.arrow_back, color: GoOutsColors.primaryBlue),
          onPressed: () => Navigator.of(context).pop(),
        ),
        title: Text(
          'Claim Notification',
          style: GoogleFonts.inter(
            fontSize: 20,
            fontWeight: FontWeight.bold,
            color: GoOutsColors.primaryBlue,
          ),
        ),
      ),
      body: SingleChildScrollView(
        child: Column(
          children: [
            _buildIntroductionBanner(),
            const SizedBox(height: 16),
            _buildCountdownTimer(),
            const SizedBox(height: 32),
            _buildClaimDetailsSection(),
            const SizedBox(height: 32),
            _buildVisualEvidenceSection(),
            const SizedBox(height: 24),
            _buildResolutionInfoBanner(),
            const SizedBox(height: 40),
          ],
        ),
      ),
      bottomNavigationBar: _buildBottomActions(),
    );
  }

  Widget _buildIntroductionBanner() {
    return Container(
      margin: const EdgeInsets.all(16),
      padding: const EdgeInsets.all(20),
      decoration: BoxDecoration(
        color: GoOutsColors.infoBlueBg,
        borderRadius: BorderRadius.circular(12),
        border: const Border(
          left: BorderSide(color: GoOutsColors.primaryBlue, width: 4),
        ),
      ),
      child: RichText(
        text: TextSpan(
          style: GoogleFonts.inter(
            fontSize: 15,
            color: GoOutsColors.bodyText,
            height: 1.5,
          ),
          children: [
            const TextSpan(text: 'Your host, Alex, has submitted a claim regarding your recent stay at '),
            TextSpan(
              text: 'The Riverside Loft',
              style: GoogleFonts.inter(fontWeight: FontWeight.bold, color: GoOutsColors.deepNavy),
            ),
            const TextSpan(text: '. Please review the details below.'),
          ],
        ),
      ),
    );
  }

  Widget _buildCountdownTimer() {
    return Container(
      padding: const EdgeInsets.symmetric(horizontal: 20, vertical: 12),
      decoration: BoxDecoration(
        color: GoOutsColors.timerPurpleBg,
        borderRadius: BorderRadius.circular(30),
      ),
      child: Row(
        mainAxisSize: MainAxisSize.min,
        children: [
          const Icon(Icons.access_time, size: 20, color: GoOutsColors.timerPurpleText),
          const SizedBox(width: 10),
          Text(
            '3 days 4 hours left to respond',
            style: GoogleFonts.inter(
              fontSize: 14,
              fontWeight: FontWeight.w600,
              color: GoOutsColors.timerPurpleText,
            ),
          ),
        ],
      ),
    );
  }

  Widget _buildClaimDetailsSection() {
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 20),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            'CLAIM DETAILS',
            style: GoogleFonts.inter(
              fontSize: 13,
              fontWeight: FontWeight.w800,
              color: GoOutsColors.bodyText.withValues(alpha: 0.7),
              letterSpacing: 1.0,
            ),
          ),
          const SizedBox(height: 20),
          Row(
            children: [
              Expanded(child: _buildDetailItem('Room', 'Main Bedroom')),
              Expanded(child: _buildDetailItem('Amount', '£45.00', isAmount: true)),
            ],
          ),
          const SizedBox(height: 24),
          _buildDetailItem('Item / Issue', 'Carpet Stain'),
        ],
      ),
    );
  }

  Widget _buildDetailItem(String label, String value, {bool isAmount = false}) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(
          label,
          style: GoogleFonts.inter(
            fontSize: 14,
            color: GoOutsColors.bodyText,
          ),
        ),
        const SizedBox(height: 4),
        Text(
          value,
          style: GoogleFonts.inter(
            fontSize: 18,
            fontWeight: FontWeight.bold,
            color: isAmount ? GoOutsColors.primaryBlue : GoOutsColors.deepNavy,
          ),
        ),
      ],
    );
  }

  Widget _buildVisualEvidenceSection() {
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 20),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            'VISUAL EVIDENCE',
            style: GoogleFonts.inter(
              fontSize: 13,
              fontWeight: FontWeight.w800,
              color: GoOutsColors.bodyText.withValues(alpha: 0.7),
              letterSpacing: 1.0,
            ),
          ),
          const SizedBox(height: 16),
          Row(
            children: [
              Expanded(
                child: _buildEvidenceCard(
                  'HOST CLAIM',
                  'https://images.unsplash.com/photo-1581578731522-7452f96921bc?auto=format&fit=crop&q=80&w=400',
                  isHost: true,
                ),
              ),
              const SizedBox(width: 12),
              Expanded(
                child: _buildEvidenceCard(
                  'YOUR ARRIVAL',
                  'https://images.unsplash.com/photo-1522771739844-6a9f6d5f14af?auto=format&fit=crop&q=80&w=400',
                  isHost: false,
                ),
              ),
            ],
          ),
          const SizedBox(height: 16),
          Center(
            child: Text(
              'Tap photos to expand and compare details',
              style: GoogleFonts.inter(
                fontSize: 13,
                fontStyle: FontStyle.italic,
                color: GoOutsColors.bodyText,
              ),
            ),
          ),
        ],
      ),
    );
  }

  Widget _buildEvidenceCard(String label, String imageUrl, {required bool isHost}) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Stack(
          children: [
            Container(
              height: 160,
              decoration: BoxDecoration(
                borderRadius: BorderRadius.circular(12),
                border: Border.all(
                  color: isHost ? Colors.transparent : GoOutsColors.primaryBlue,
                  width: isHost ? 0 : 2,
                ),
                image: DecorationImage(
                  image: NetworkImage(imageUrl),
                  fit: BoxFit.cover,
                ),
              ),
            ),
            Positioned(
              top: 8,
              left: 8,
              child: Container(
                padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
                decoration: BoxDecoration(
                  color: isHost ? const Color(0xFF0D1B3E).withValues(alpha: 0.7) : GoOutsColors.primaryBlue.withValues(alpha: 0.9),
                  borderRadius: BorderRadius.circular(4),
                ),
                child: Text(
                  label,
                  style: GoogleFonts.inter(
                    fontSize: 10,
                    fontWeight: FontWeight.w800,
                    color: Colors.white,
                    letterSpacing: 0.5,
                  ),
                ),
              ),
            ),
          ],
        ),
      ],
    );
  }

  Widget _buildResolutionInfoBanner() {
    return Container(
      margin: const EdgeInsets.symmetric(horizontal: 20),
      padding: const EdgeInsets.all(16),
      decoration: BoxDecoration(
        color: GoOutsColors.infoBlueBg,
        borderRadius: BorderRadius.circular(12),
      ),
      child: Row(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          const Icon(Icons.info_outline, color: GoOutsColors.primaryBlue, size: 24),
          const SizedBox(width: 16),
          Expanded(
            child: Text(
              'If you contest this claim, our resolution team will review both sets of time-stamped photographs to make a final determination within 24 hours.',
              style: GoogleFonts.inter(
                fontSize: 14,
                color: GoOutsColors.bodyText,
                height: 1.4,
              ),
            ),
          ),
        ],
      ),
    );
  }

  Widget _buildBottomActions() {
    return Container(
      padding: const EdgeInsets.fromLTRB(20, 20, 20, 40),
      decoration: const BoxDecoration(
        color: Colors.white,
        border: Border(top: BorderSide(color: GoOutsColors.dividerGray)),
      ),
      child: Row(
        children: [
          Expanded(
            child: ElevatedButton(
              onPressed: () {},
              style: ElevatedButton.styleFrom(
                backgroundColor: GoOutsColors.acceptBlue,
                foregroundColor: GoOutsColors.deepNavy,
                minimumSize: const Size(double.infinity, 56),
                shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                elevation: 0,
              ),
              child: Text(
                'Accept',
                style: GoogleFonts.inter(fontSize: 18, fontWeight: FontWeight.bold),
              ),
            ),
          ),
          const SizedBox(width: 12),
          Expanded(
            child: ElevatedButton(
              onPressed: () {},
              style: ElevatedButton.styleFrom(
                backgroundColor: GoOutsColors.contestBlue,
                foregroundColor: Colors.white,
                minimumSize: const Size(double.infinity, 56),
                shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                elevation: 0,
              ),
              child: Text(
                'Contest',
                style: GoogleFonts.inter(fontSize: 18, fontWeight: FontWeight.bold),
              ),
            ),
          ),
        ],
      ),
    );
  }
}
