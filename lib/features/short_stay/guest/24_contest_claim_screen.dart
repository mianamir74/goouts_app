// Generated by Google Stitch, 3 August 2026. Source folder: 24-contest_claim
// Cleaned and consolidated by the pass described in
// goouts/design/SHORT_STAY_WIRING_PLAN.md part 1.
// NOT WIRED. Every handler in this file is still empty. See part 2.
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';
import '../theme/stay_colors.dart';

class ContestClaimScreen extends StatelessWidget {
  const ContestClaimScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: GoOutsColors.background,
      appBar: AppBar(
        backgroundColor: Colors.white,
        elevation: 0.5,
        leading: IconButton(
          icon: const Icon(Icons.arrow_back, color: GoOutsColors.primaryBlue),
          onPressed: () => Navigator.of(context).pop(),
        ),
        title: Text(
          'Contest Claim',
          style: GoogleFonts.inter(
            fontSize: 20,
            fontWeight: FontWeight.bold,
            color: GoOutsColors.primaryBlue,
          ),
        ),
        actions: [
          IconButton(
            icon: const Icon(Icons.account_circle_outlined, color: GoOutsColors.primaryBlue),
            onPressed: () {},
          ),
        ],
      ),
      body: SingleChildScrollView(
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            _buildDecisionGuidelines(),
            _buildResponseSection(),
            _buildSupportingEvidence(),
            _buildSubmitSection(),
            const SizedBox(height: 40),
          ],
        ),
      ),
      bottomNavigationBar: _buildBottomNavBar(),
    );
  }

  Widget _buildDecisionGuidelines() {
    return Container(
      margin: const EdgeInsets.all(16),
      padding: const EdgeInsets.all(20),
      decoration: BoxDecoration(
        color: GoOutsColors.infoBlueBg,
        borderRadius: BorderRadius.circular(12),
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Row(
            children: [
              const Icon(Icons.info_outline, color: GoOutsColors.primaryBlue, size: 24),
              const SizedBox(width: 12),
              Text(
                'How this is decided',
                style: GoogleFonts.inter(
                  fontSize: 18,
                  fontWeight: FontWeight.bold,
                  color: GoOutsColors.deepNavy,
                ),
              ),
            ],
          ),
          const SizedBox(height: 16),
          _buildGuidelineItem(Icons.check_circle_outline, 'Matched before/after photos carry the most weight in our resolution process.'),
          const SizedBox(height: 12),
          _buildGuidelineItem(Icons.verified_user_outlined, 'Normal wear and tear is never charged to the guest; we value factual clarity.'),
          const SizedBox(height: 12),
          _buildGuidelineItem(Icons.account_balance_wallet_outlined, 'Damage is paid on remaining life rather than replacement cost to ensure fairness.'),
        ],
      ),
    );
  }

  Widget _buildGuidelineItem(IconData icon, String text) {
    return Row(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Icon(icon, color: GoOutsColors.primaryBlue, size: 20),
        const SizedBox(width: 12),
        Expanded(
          child: Text(
            text,
            style: GoogleFonts.inter(
              fontSize: 14,
              color: GoOutsColors.bodyText,
              height: 1.4,
            ),
          ),
        ),
      ],
    );
  }

  Widget _buildResponseSection() {
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 16),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            'Your Response',
            style: GoogleFonts.inter(
              fontSize: 18,
              fontWeight: FontWeight.bold,
              color: GoOutsColors.deepNavy,
            ),
          ),
          const SizedBox(height: 12),
          Container(
            height: 160,
            padding: const EdgeInsets.all(16),
            decoration: BoxDecoration(
              color: Colors.white,
              borderRadius: BorderRadius.circular(12),
              border: Border.all(color: GoOutsColors.outlineVariant.withValues(alpha: 0.5)),
            ),
            child: TextField(
              maxLines: null,
              decoration: InputDecoration(
                hintText: 'Provide details on why you are contesting this claim...',
                hintStyle: GoogleFonts.inter(color: GoOutsColors.bodyText.withValues(alpha: 0.6), fontSize: 15),
                border: InputBorder.none,
              ),
            ),
          ),
          const SizedBox(height: 8),
          Row(
            mainAxisAlignment: MainAxisAlignment.spaceBetween,
            children: [
              Text(
                'Minimum 50 words recommended',
                style: GoogleFonts.inter(fontSize: 13, color: GoOutsColors.bodyText.withValues(alpha: 0.7)),
              ),
              Text(
                '0 / 2000',
                style: GoogleFonts.inter(fontSize: 13, color: GoOutsColors.bodyText.withValues(alpha: 0.7)),
              ),
            ],
          ),
        ],
      ),
    );
  }

  Widget _buildSupportingEvidence() {
    return Padding(
      padding: const EdgeInsets.all(16),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Row(
            mainAxisAlignment: MainAxisAlignment.spaceBetween,
            children: [
              Text(
                'Supporting Evidence',
                style: GoogleFonts.inter(
                  fontSize: 18,
                  fontWeight: FontWeight.bold,
                  color: GoOutsColors.deepNavy,
                ),
              ),
              TextButton(
                onPressed: () {},
                child: Text(
                  'Select All',
                  style: GoogleFonts.inter(
                    fontSize: 14,
                    fontWeight: FontWeight.bold,
                    color: GoOutsColors.primaryBlue,
                  ),
                ),
              ),
            ],
          ),
          const SizedBox(height: 8),
          Text(
            'Add photographs from pack',
            style: GoogleFonts.inter(fontSize: 15, color: GoOutsColors.bodyText),
          ),
          const SizedBox(height: 16),
          SizedBox(
            height: 100,
            child: ListView(
              scrollDirection: Axis.horizontal,
              children: [
                _buildEvidenceThumb('https://images.unsplash.com/photo-1581578731522-7452f96921bc?auto=format&fit=crop&q=80&w=200', isSelected: true),
                const SizedBox(width: 12),
                _buildEvidenceThumb('https://images.unsplash.com/photo-1556911220-e15b29be8c8f?auto=format&fit=crop&q=80&w=200'),
                const SizedBox(width: 12),
                _buildEvidenceThumb('https://images.unsplash.com/photo-1505691723518-36a5ac3be353?auto=format&fit=crop&q=80&w=200'),
                const SizedBox(width: 12),
                _buildUploadThumb(),
              ],
            ),
          ),
        ],
      ),
    );
  }

  Widget _buildEvidenceThumb(String imageUrl, {bool isSelected = false}) {
    return Stack(
      children: [
        ClipRRect(
          borderRadius: BorderRadius.circular(8),
          child: Image.network(imageUrl, width: 100, height: 100, fit: BoxFit.cover),
        ),
        if (isSelected)
          Positioned(
            top: 4,
            right: 4,
            child: Container(
              padding: const EdgeInsets.all(2),
              decoration: const BoxDecoration(color: GoOutsColors.primaryBlue, shape: BoxShape.circle),
              child: const Icon(Icons.check, size: 14, color: Colors.white),
            ),
          ),
        if (isSelected)
          Container(
            width: 100,
            height: 100,
            decoration: BoxDecoration(
              borderRadius: BorderRadius.circular(8),
              border: Border.all(color: GoOutsColors.primaryBlue, width: 2),
            ),
          ),
      ],
    );
  }

  Widget _buildUploadThumb() {
    return Container(
      width: 100,
      height: 100,
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(8),
        border: Border.all(color: GoOutsColors.outlineVariant, style: BorderStyle.solid),
      ),
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          const Icon(Icons.camera_alt_outlined, color: GoOutsColors.outlineVariant),
          const SizedBox(height: 4),
          Text(
            'Upload',
            style: GoogleFonts.inter(fontSize: 12, color: GoOutsColors.outlineVariant, fontWeight: FontWeight.bold),
          ),
        ],
      ),
    );
  }

  Widget _buildSubmitSection() {
    return Padding(
      padding: const EdgeInsets.all(16),
      child: Column(
        children: [
          ElevatedButton(
            onPressed: () {},
            style: ElevatedButton.styleFrom(
              backgroundColor: GoOutsColors.primaryBlue,
              minimumSize: const Size(double.infinity, 56),
              shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
              elevation: 0,
            ),
            child: Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                Text(
                  'Submit Contest',
                  style: GoogleFonts.inter(fontSize: 18, fontWeight: FontWeight.bold, color: Colors.white),
                ),
                const SizedBox(width: 8),
                const Icon(Icons.send, color: Colors.white, size: 20),
              ],
            ),
          ),
          const SizedBox(height: 16),
          Text(
            'By submitting, you agree to the StaySure Dispute Terms & Conditions.',
            textAlign: TextAlign.center,
            style: GoogleFonts.inter(fontSize: 12, color: GoOutsColors.bodyText.withValues(alpha: 0.7)),
          ),
        ],
      ),
    );
  }

  Widget _buildBottomNavBar() {
    return Container(
      height: 80,
      decoration: const BoxDecoration(
        color: Colors.white,
        border: Border(top: BorderSide(color: GoOutsColors.dividerGray)),
      ),
      child: Row(
        mainAxisAlignment: MainAxisAlignment.spaceAround,
        children: [
          _buildNavItem(Icons.search, 'Search', false),
          _buildNavItem(Icons.calendar_today, 'Bookings', false),
          _buildNavItem(Icons.camera_alt, 'Deposit', false),
          _buildNavItem(Icons.gavel, 'Disputes', true),
          _buildNavItem(Icons.person, 'Profile', false),
        ],
      ),
    );
  }

  Widget _buildNavItem(IconData icon, String label, bool isActive) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Icon(icon, color: isActive ? GoOutsColors.primaryBlue : GoOutsColors.bodyText),
        const SizedBox(height: 4),
        Text(
          label,
          style: GoogleFonts.inter(
            fontSize: 10,
            fontWeight: isActive ? FontWeight.bold : FontWeight.w500,
            color: isActive ? GoOutsColors.primaryBlue : GoOutsColors.bodyText,
          ),
        ),
      ],
    );
  }
}
