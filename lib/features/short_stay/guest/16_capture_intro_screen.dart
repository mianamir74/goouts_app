// Generated by Google Stitch, 3 August 2026. Source folder: 16-protect_your_deposit
//
// WIRED 16 August 2026, task #116, Phase D.
//
// The one handler on this screen — Start — was `onPressed: () {}`, so the
// screen that exists to talk a guest into photographing the property could not
// take them to the camera. It now opens the capture flow for this booking.
//
// It also takes a `kind`, because the same explanation serves arrival and
// departure. Departure is where guests are most likely to skip, being on their
// way out of the door, and it is the set that decides a claim.
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';
import '../models/stay_enums.dart';
import '../stay_routes.dart';
import '../theme/stay_colors.dart';

class DepositProtectionScreen extends StatelessWidget {
  const DepositProtectionScreen({
    super.key,
    required this.bookingId,
    this.kind = CaptureKind.guestCheckIn,
  });

  final String bookingId;
  final CaptureKind kind;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: GoOutsColors.background,
      appBar: AppBar(
        backgroundColor: Colors.white,
        elevation: 0.5,
        leading: IconButton(
          icon: const Icon(Icons.arrow_back, color: GoOutsColors.deepNavy),
          onPressed: () => Navigator.of(context).pop(),
        ),
        title: Text(
          'Deposit Protection',
          style: GoogleFonts.inter(
            fontSize: 20,
            fontWeight: FontWeight.bold,
            color: GoOutsColors.deepNavy,
          ),
        ),
        centerTitle: true,
      ),
      body: SingleChildScrollView(
        child: Column(
          children: [
            _buildHeroImage(),
            Padding(
              padding: const EdgeInsets.symmetric(horizontal: 24.0),
              child: Column(
                children: [
                  const SizedBox(height: 32),
                  Text(
                    'Protect your deposit',
                    style: GoogleFonts.inter(
                      fontSize: 32,
                      fontWeight: FontWeight.bold,
                      color: GoOutsColors.deepNavy,
                    ),
                  ),
                  const SizedBox(height: 40),
                  _buildFeatureRow(
                    Icons.access_time_filled,
                    'Takes about a minute',
                    'Quick and easy process',
                  ),
                  const SizedBox(height: 32),
                  _buildFeatureRow(
                    Icons.camera_alt,
                    'Records the property',
                    'Capture the state before you settle in',
                  ),
                  const SizedBox(height: 32),
                  _buildFeatureRow(
                    Icons.verified_user,
                    'Protects against claims',
                    'Evidence against existing damage claims',
                  ),
                  const SizedBox(height: 40),
                  _buildInfoBanner(),
                  const SizedBox(height: 32),
                  _buildStartButton(context),
                  const SizedBox(height: 40),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }

  Widget _buildHeroImage() {
    return Container(
      width: double.infinity,
      height: 240,
      margin: const EdgeInsets.fromLTRB(16, 16, 16, 0),
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(24),
        image: const DecorationImage(
          image: NetworkImage(
            'https://images.unsplash.com/photo-1512917774080-9991f1c4c750?auto=format&fit=crop&q=80&w=800',
          ),
          fit: BoxFit.cover,
        ),
      ),
    );
  }

  Widget _buildFeatureRow(IconData icon, String title, String subtitle) {
    return Row(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Container(
          padding: const EdgeInsets.all(12),
          decoration: BoxDecoration(
            color: const Color(0xFFD0F0FF),
            borderRadius: BorderRadius.circular(12),
          ),
          child: Icon(icon, color: GoOutsColors.iconBlue, size: 28),
        ),
        const SizedBox(width: 20),
        Expanded(
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Text(
                title,
                style: GoogleFonts.inter(
                  fontSize: 18,
                  fontWeight: FontWeight.bold,
                  color: GoOutsColors.deepNavy,
                ),
              ),
              const SizedBox(height: 4),
              Text(
                subtitle,
                style: GoogleFonts.inter(
                  fontSize: 15,
                  color: GoOutsColors.bodyText,
                ),
              ),
            ],
          ),
        ),
      ],
    );
  }

  Widget _buildInfoBanner() {
    return Container(
      padding: const EdgeInsets.all(16),
      decoration: BoxDecoration(
        color: GoOutsColors.infoBlueBg.withValues(alpha: 0.5),
        borderRadius: BorderRadius.circular(12),
      ),
      child: Row(
        children: [
          const Icon(Icons.info_outline, color: GoOutsColors.primaryBlue, size: 20),
          const SizedBox(width: 12),
          Expanded(
            child: Text(
              'Your host will receive these photos for record-keeping.',
              style: GoogleFonts.inter(
                fontSize: 14,
                color: GoOutsColors.bodyText,
                fontWeight: FontWeight.w500,
              ),
            ),
          ),
        ],
      ),
    );
  }

  Widget _buildStartButton(BuildContext context) {
    return ElevatedButton(
      // pushReplacement: the intro is an explanation, not a step. Coming back
      // from the checklist should return to the trip, not re-read the pitch.
      onPressed: () => Navigator.of(context).pushReplacementNamed(
        StayRoutes.captureChecklist,
        arguments: <String, dynamic>{
          'bookingId': bookingId,
          'captureKind': kind.wire,
        },
      ),
      style: ElevatedButton.styleFrom(
        backgroundColor: const Color(0xFF00668F),
        minimumSize: const Size(double.infinity, 56),
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(28)),
        elevation: 0,
      ),
      child: Text(
        'Start',
        style: GoogleFonts.inter(
          fontSize: 18,
          fontWeight: FontWeight.bold,
          color: Colors.white,
        ),
      ),
    );
  }
}
