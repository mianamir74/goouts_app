// Generated by Google Stitch, 3 August 2026. Source folder: 22-evidence_pack
// Cleaned and consolidated by the pass described in
// goouts/design/SHORT_STAY_WIRING_PLAN.md part 1.
// NOT WIRED. Every handler in this file is still empty. See part 2.
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';
import '../theme/stay_colors.dart';

class EvidencePackScreen extends StatelessWidget {
  const EvidencePackScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return DefaultTabController(
      length: 3,
      child: Scaffold(
        backgroundColor: GoOutsColors.background,
        appBar: AppBar(
          backgroundColor: Colors.white,
          elevation: 0.5,
          leading: IconButton(
            icon: const Icon(Icons.arrow_back, color: GoOutsColors.primaryBlue),
            onPressed: () => Navigator.of(context).pop(),
          ),
          title: Text(
            'Evidence Pack',
            style: GoogleFonts.inter(
              fontSize: 20,
              fontWeight: FontWeight.bold,
              color: GoOutsColors.deepNavy,
            ),
          ),
          actions: [
            IconButton(
              icon: const Icon(Icons.account_circle_outlined, color: GoOutsColors.primaryBlue),
              onPressed: () {},
            ),
          ],
          bottom: TabBar(
            isScrollable: true,
            indicatorColor: GoOutsColors.primaryBlue,
            indicatorWeight: 3,
            labelColor: GoOutsColors.primaryBlue,
            unselectedLabelColor: GoOutsColors.bodyText,
            labelStyle: GoogleFonts.inter(fontWeight: FontWeight.bold, fontSize: 15),
            tabs: const [
              Tab(text: 'Host (Pre-arrival)'),
              Tab(text: 'You (Arrival)'),
              Tab(text: 'You (Departure)'),
            ],
          ),
        ),
        body: TabBarView(
          children: [
            _buildEvidenceList(),
            const Center(child: Text('No arrival photos yet')),
            const Center(child: Text('No departure photos yet')),
          ],
        ),
      ),
    );
  }

  Widget _buildEvidenceList() {
    return ListView(
      padding: const EdgeInsets.all(16),
      children: [
        _buildSecurityBanner(),
        const SizedBox(height: 24),
        _buildRoomSection(
          'Living Room',
          '4 images captured',
          'https://images.unsplash.com/photo-1512917774080-9991f1c4c750?auto=format&fit=crop&q=80&w=800',
          timestamp: '2023-10-12 14:02:11 UTC',
          isVerified: true,
        ),
        const SizedBox(height: 16),
        Row(
          children: [
            Expanded(
              child: _buildRoomSection(
                'Kitchen',
                'Verified clean',
                'https://images.unsplash.com/photo-1556911220-e15b29be8c8f?auto=format&fit=crop&q=80&w=400',
                timestamp: '14:05:45',
              ),
            ),
            const SizedBox(width: 12),
            Expanded(
              child: _buildRoomSection(
                'Bedroom',
                'All surfaces checked',
                'https://images.unsplash.com/photo-1505691723518-36a5ac3be353?auto=format&fit=crop&q=80&w=400',
                timestamp: '14:08:22',
              ),
            ),
          ],
        ),
        const SizedBox(height: 16),
        _buildRoomSection(
          'Bathroom & En-suite',
          'Sanitized evidence active',
          'https://images.unsplash.com/photo-1584622650111-993a426fbf0a?auto=format&fit=crop&q=80&w=800',
          timestamp: '2023-10-12 14:12:05 UTC',
          isVerified: true,
        ),
        const SizedBox(height: 32),
        _buildDownloadButton(),
        const SizedBox(height: 12),
        _buildHashFooter(),
        const SizedBox(height: 40),
      ],
    );
  }

  Widget _buildSecurityBanner() {
    return Container(
      padding: const EdgeInsets.all(16),
      decoration: BoxDecoration(
        color: GoOutsColors.infoBlueBg,
        borderRadius: BorderRadius.circular(12),
      ),
      child: Row(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          const Icon(Icons.lock_outline, color: GoOutsColors.primaryBlue, size: 24),
          const SizedBox(width: 16),
          Expanded(
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text(
                  'Immutable Security',
                  style: GoogleFonts.inter(
                    fontSize: 16,
                    fontWeight: FontWeight.bold,
                    color: GoOutsColors.primaryBlue,
                  ),
                ),
                const SizedBox(height: 4),
                Text(
                  'Nobody can edit or delete these, including us. Photos are cryptographically signed and timestamped.',
                  style: GoogleFonts.inter(
                    fontSize: 14,
                    color: GoOutsColors.bodyText,
                    height: 1.4,
                  ),
                ),
              ],
            ),
          ),
        ],
      ),
    );
  }

  Widget _buildRoomSection(
    String title,
    String subtitle,
    String imageUrl, {
    required String timestamp,
    bool isVerified = false,
  }) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Stack(
          children: [
            ClipRRect(
              borderRadius: BorderRadius.circular(12),
              child: Image.network(
                imageUrl,
                height: title.contains('&') ? 200 : 160,
                width: double.infinity,
                fit: BoxFit.cover,
              ),
            ),
            Positioned(
              bottom: 8,
              left: 8,
              child: Container(
                padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
                decoration: BoxDecoration(
                  color: Colors.black.withValues(alpha: 0.6),
                  borderRadius: BorderRadius.circular(4),
                ),
                child: Text(
                  'TS: $timestamp',
                  style: GoogleFonts.inter(
                    fontSize: 10,
                    fontWeight: FontWeight.bold,
                    color: Colors.white,
                  ),
                ),
              ),
            ),
          ],
        ),
        const SizedBox(height: 12),
        Row(
          mainAxisAlignment: MainAxisAlignment.spaceBetween,
          children: [
            Expanded(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Text(
                    title,
                    style: GoogleFonts.inter(
                      fontSize: 16,
                      fontWeight: FontWeight.bold,
                      color: GoOutsColors.deepNavy,
                    ),
                  ),
                  Text(
                    subtitle,
                    style: GoogleFonts.inter(
                      fontSize: 14,
                      color: GoOutsColors.bodyText,
                    ),
                  ),
                ],
              ),
            ),
            Icon(
              isVerified ? Icons.check_circle_outline : Icons.verified_outlined,
              size: 20,
              color: isVerified ? GoOutsColors.primaryBlue : GoOutsColors.bodyText.withValues(alpha: 0.5),
            ),
          ],
        ),
      ],
    );
  }

  Widget _buildDownloadButton() {
    return ElevatedButton.icon(
      onPressed: () {},
      icon: const Icon(Icons.file_download_outlined, color: Colors.white),
      label: Text(
        'Download PDF Report',
        style: GoogleFonts.inter(
          fontSize: 16,
          fontWeight: FontWeight.bold,
          color: Colors.white,
        ),
      ),
      style: ElevatedButton.styleFrom(
        backgroundColor: GoOutsColors.primaryBlue,
        minimumSize: const Size(double.infinity, 56),
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
        elevation: 0,
      ),
    );
  }

  Widget _buildHashFooter() {
    return Center(
      child: Text(
        'Hash: 8a4f...2d1e | SHA-256 Verified',
        style: GoogleFonts.inter(
          fontSize: 12,
          color: GoOutsColors.bodyText.withValues(alpha: 0.6),
          fontWeight: FontWeight.w500,
        ),
      ),
    );
  }
}
